"""
.remove()
Removes the first occurrence of a specified value from a list.

.pop()
Removes and returns an item from a list. It removes by index (or the last item if no index is given), while .remove() removes by value.

.sort()
Sorts the items in a list in ascending order by default.

.append()
Adds a single item to the end of a list.

.insert()
Adds an item at a specific position in a list, while .append() always adds an item to the end of the list.
"""
